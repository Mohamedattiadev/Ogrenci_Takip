# Bos/tanimsiz birakilirsa lib/api.ts goreli '/api/v1' kullanir - istekler ayni origin'e
# gider ve next.config.ts'teki rewrite bunlari API_PROXY_TARGET'a (varsayilan
# http://localhost:5001) yonlendirir. Sadece API'yi FARKLI bir origin'de servis
# ediyorsanız (ör. prod) burayi tam URL ile doldurun.
NEXT_PUBLIC_API_URL=
